apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hdfs-log-anomaly
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/your-org/hdfs-log-anomaly-platform.git
    targetRevision: main
    path: helm/hdfs-log-anomaly
  destination:
    server: https://kubernetes.default.svc
    namespace: hdfs-observability
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
